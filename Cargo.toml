[workspace]
resolver = "2"
members = [
    "crates/ndn-pib-idb",
    "crates/ndn-acme",
    "crates/tooling/ndn-tools-core",
    "binaries/tooling/did-ndn-driver",
    "binaries/ndn-fwd",
    "binaries/tooling/ndn-tools",
    "binaries/tooling/ndn-bench",
    "binaries/tooling/enroll-ndncert",
    "binaries/tooling/ndn-pair-kiosk",
    "binaries/ndn-fwd-tokens",
    "binaries/ndn-trust-context",
    "binaries/tooling/audit-strict-mgmt-validation",
    "binaries/tooling/ndn-otel-bridge",
]
default-members = [
    "crates/ndn-pib-idb",
    "crates/ndn-acme",
    "crates/tooling/ndn-tools-core",
    "binaries/tooling/did-ndn-driver",
    "binaries/ndn-fwd",
    "binaries/tooling/ndn-tools",
    "binaries/tooling/ndn-bench",
    "binaries/tooling/enroll-ndncert",
    "binaries/tooling/ndn-pair-kiosk",
    "binaries/ndn-fwd-tokens",
    "binaries/ndn-trust-context",
    "binaries/tooling/audit-strict-mgmt-validation",
    "binaries/tooling/ndn-otel-bridge",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
# MSRV — verified in CI by the `msrv` job; bump deliberately with a matching CI
# run. Mirrors the ndn-rs floor (edition 2024 + fjall's declared 1.90).
rust-version = "1.90"
authors = ["ndn-rs contributors"]
license = "MIT OR Apache-2.0"

# Single source of truth for lint policy — every member inherits via
# `[lints] workspace = true`. CI escalates warn → error with -D warnings, so
# "warn" here means "error at the gate, advisory while hacking". ndn-fwd is
# unsafe-free (the OS-I/O unsafe lives in sibling ndn-ext leaf crates), so
# `deny(unsafe_code)` holds workspace-wide with no exceptions.
[workspace.lints.rust]
unsafe_code = "deny"

[workspace.lints.clippy]
dbg_macro = "warn"
todo = "warn"
unimplemented = "warn"

[workspace.metadata.crane]
name = "ndn-fwd"

[workspace.dependencies]
# NDN workspace crates — grouped by subdirectory layer
ndn-packet    = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3", default-features = false }
ndn-mgmt-wire = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-transport = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-store     = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-trust-envelope = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face             = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-face-serial      = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-websocket   = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-shm         = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-bluetooth   = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-afxdp       = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-webtransport = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-quic         = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-webrtc       = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-rtc-signaling-relay = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
# The named-radio medium face ("the medium is the face"): one face over N radio
# capabilities, driven by the medium-shaped cognitive control plane.
ndn-face-monitor-wifi = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-radio-cognition   = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-acme              = { path = "crates/ndn-acme" }
ndn-strategy  = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-security  = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-engine    = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-app       = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-ipc       = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-ipc-shm   = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-config    = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-discovery = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3", default-features = false }
ndn-routing    = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-sync      = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-cert     = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-identity = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-mgmt     = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-observability = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-coding    = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-strategy-cclf = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3", default-features = false }
ndn-ratelimit = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-tools-core = { path = "crates/tooling/ndn-tools-core" }

# Core async runtime — individual crates enable only the features they need
tokio      = { version = "1", default-features = false }
tokio-util = { version = "0.7.10", default-features = false }

# Zero-copy byte buffers (no default features — crates opt in to bytes/std explicitly)
bytes = { version = "1", default-features = false }

# Concurrent data structures
dashmap  = "6"

# Structured logging
tracing = "0.1"
# tokio-console: runtime task inspector (opt-in; requires --cfg tokio_unstable)
console-subscriber = "0.4"

# Async combinators and stream/sink extension traits
futures = "0.3"
async-trait = "0.1"

# Error handling
anyhow    = "1"
thiserror = "2"
libc = "0.2"

# Serialization
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = { version = "0.22", default-features = false, features = ["alloc"] }

# Cryptography (digest hashing for cert/token surfaces)
sha2          = "0.11"

# WebSocket support
tokio-tungstenite = { version = "0.26" }

# ACME (RFC 8555) — used by ndn-acme to provision real certs for the
# WebSocket-TLS face (issue #3) and the WebTransport listener (issue #14).
instant-acme = { version = "0.7" }
# DNS-01 challenge plumbing — Cloudflare HTTP API is the only impl shipped
# in phase 2; rustls + reqwest are scoped to ndn-acme so the WT face crate
# stays small.
reqwest      = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }
# X.509 parsing — ndn-acme reads the leaf cert's notAfter to drive renewal
# and the cert-status surface.
x509-parser  = { version = "0.18" }

# WebSocket TLS (server-side)
rcgen             = { version = "0.13", default-features = false, features = ["pem", "crypto", "ring"] }
rustls-pemfile    = { version = "2" }

# Logging subscriber (binaries only)
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# ── Build profiles ─────────────────────────────────────────────────────────────

# console: build ndn-fwd with tokio-console task inspector support.
#
# Note: `profile-rustflags` is not yet stable in Cargo; set the required
# --cfg tokio_unstable via the environment instead:
#
#   RUSTFLAGS="--cfg tokio_unstable" cargo build -p ndn-fwd --features console --profile console
#
# The profile inherits dev settings; add debug symbols and disable
# inlining to keep tokio-console task names readable.
[profile.release]
strip = "symbols"
lto = "thin"
codegen-units = 1

[profile.console]
inherits = "dev"
debug = true
opt-level = 0
